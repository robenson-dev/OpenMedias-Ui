import React, {FC} from "react";

const SectionSearchBar: FC = () => {
    return (
        <div className="search-bar">
            <input type="text" placeholder="Search..."/>
        </div>
    );
}

export default SectionSearchBar;